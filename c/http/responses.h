#ifndef RESPONSES_H
#define RESPONSES_H

// Declaração da função que retornar o HTML.
// Outros arquivos que incluírem "responses.h" saberão que essa função existe.
const char *get_root_html_response();

#endif
